import React, { useEffect } from 'react';

const CreateGradebook = () => {
    const [inputData, setInputData] = useEffect({
        name: ""
    });
  return (
    <div>
      Create gradebook
    </div>
  )
}

export default CreateGradebook;
